<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:webfeeds="http://webfeeds.org/rss/1.0" version="2.0"><channel><title>morganherlocker</title><link>http://morganherlocker.com</link><description>website of @morganherlocker</description><item><title>Fast data websites</title><dc:creator>Morgan Herlocker</dc:creator><pubDate>Tue, 22 Jul 2025 07:00:00 GMT</pubDate><link>http://morganherlocker.com/post/Fast-data-websites</link><guid isPermaLink="true">http://morganherlocker.com/post/Fast-data-websites</guid><description><![CDATA[<p>Websites designed around the display of data often suffer from poor performance. This post will explore how to avoid this using the primitives of the browser, by changing how we think about data.</p>
<p>A minimalist-styled website only requires plain HTML with a bit of vanilla JS for interactivity. This is not an exhaustive list, but these tips are usually sufficient for writing websites that are perceived as shockingly fast to users:</p>
<ul>
<li>Don&#39;t allocate objects at runtime after load (preallocate everything). </li>
<li>Use typed arrays anywhere you need to track more than a handful of things. </li>
<li>Don&#39;t write string-compare heavy code.</li>
<li>Don&#39;t write object-as-map/set for more than a handful of elements.</li>
<li>Flatten any large group of data into a set of typed arrays.</li>
<li>Network-fetched data should arrive in typed array compatible binary format - it&#39;s easy and a faster format parser is not possible.</li>
</ul>
<p>Learning how to do this and learning the basics of HTML, CSS, and JS takes a bit of effort, but unlike framework-stack-o-the-week, you don&#39;t have to restart every few years, since the standards change slowly, with strong backwards compatibility. Think of the browser runtime as a UI framework, like React or QT, because that&#39;s what it ultimately is.</p>
<p>Usually, when something gets &quot;ported to C&quot; or &quot;ported to Rust&quot; and you see a big speedup, it&#39;s primarily driven by following these patterns, and has very little to do with the choice of language. </p>
<h2>Latency</h2>
<p><strong><em>Why are most of the tips above about avoiding certain operations?</em> The speed at which an operation in a program can be performed is driven by the physical distance data must travel.</strong> Norvig and others have compiled helpful lists of latency numbers per operation type, like this one:</p>
<p><img src="/img/latency-nums.png" alt="latency numbers every programmer should know">
<a href="https://colin-scott.github.io/personal_website/research/interactive_latency.html">https://colin-scott.github.io/personal_website/research/interactive_latency.html</a></p>
<p>These are physical constraints, and no magic programming language design can overcome physics. Porting to WASM or vulkan or ____ won&#39;t save you, and it isn&#39;t necessary where fast primitives exist in JS. </p>
<p>Exceptions exist, such as When native intrinsic libraries like AVX are required to go beyond the performance afforded by a naive data-oriented C implementation for an additional ~5-10x speedup. In these situations, first consider sharding or aggregating your data. If that won&#39;t do, ditch the web and use a lib like SDL3 to build a crossplatform native app. </p>
<p>Don&#39;t skip ahead in this optimization order - each additional layer comes at cost to accessibility (the single most important quality of a data visualization), so consider the simpler approach carefully before moving on, but don&#39;t be afraid to either. Writing a raylib GUI in C is not that more difficult or different than writing a vanilla style data web page.</p>
<h2>Setting a baseline</h2>
<p><a href="https://www.gutenberg.org/files/2600/2600-h/2600-h.htm">War and Peace, Project Guttenberg</a> loads instantly and uses no amount of perceivable CPU on a modern device. I can cmd+f search for strings, and results return instantly. Given this, be confident that your website can comfortably display as much text as a half million+ word novel. Well before this, data visualizations should utilize aggregation for simple practicality reasons.</p>
<p>Similar examples can be thought of and measured for other types of data. For example, try running a high res video on a simple HTML page to get a feel for runtime cost of a page that rapidly updates every pixel on the screen.</p>
<p>Understand that if you&#39;re not meeting the baseline comp you&#39;ve set, there is something you have written or (more likely) a dependency that you&#39;ve taken that has blown the budget unneccessarily. Simplify and delete until you are back to baseline.</p>
<h2>Awareness of runtime</h2>
<p>When code becomes thousands of times slower than the naive implementation, it is rarely the case that a coder sat down and tried to write something fast, and the slow page was the result. Always set a baseline for the type of page being written, and periodically compare changes against this baseline. As of 2025, the performance profiling tools that come with firefox and chrome are both capable of measuring a page&#39;s performance and describing where the runtime is being spent. </p>
<p>This process is important when considering dependencies. Simply do not add dependencies that move the needle in a bad direction significantly. Do not add dependencies that take over the runtime. Do not add dependencies that obscure the runtime. <strong>Stay as close to a <code>while(true) {...}</code> update loop as you can manage.</strong> Do this, and adding small functional dependencies is not a big deal - take advantage of the npm ecosystem, and appreciate the small-module-UNIXy philosophy that drove much of early node/JS development. While small libs like leftpad are often bemoaned, small functional libs are easy to replace with a more minimal or optimized implementation in the future, while frameworks only get harder to do the same over time.</p>
<h2>Screen refresh rate</h2>
<p>Humans perceive animation when objects move at about 10 fps. It&#39;s tempting to settle here and call it a day. Instead, set a goal at the beginning of authoring a page to maintain the max screen refresh rate of a blank page. This will likely be something like 30 fps, 60 fps, or 140 fps depending on the hardware you have available to test on. Set a hard rule that you won&#39;t ever ship a change that deviates from your objective by 10%. I aim for 140 fps whenever possible, which gives 7-8 ms per frame to play with. If I really need to perform an operation that cannot fit in this budget comfortably, I move it to a worker thread to avoid UI jitter. I never add more than a couple web workers, even when I know most systems could handle it. The point is not to speed things up, but to simply move blocking work off the UI thread. Use the same judgement for these tasks that you would for a networked fetch call, because latency will be in a similar ballpark (10-1000 ms).</p>
<p><strong>If you take away nothing else from this, take away that you should always maintain a minimum of 60 fps when writing a web page. Don&#39;t do things that make you unsure if you can or have achieved this.</strong></p>
<hr>
<p><em>7-22-25</em></p>
]]></description></item><item><title>Worm Helsing</title><dc:creator>Morgan Herlocker</dc:creator><pubDate>Sat, 31 May 2025 07:00:00 GMT</pubDate><link>http://morganherlocker.com/post/Worm-Helsing</link><guid isPermaLink="true">http://morganherlocker.com/post/Worm-Helsing</guid><description><![CDATA[<p>Today my friend, <a href="http://xoxodan.com/">Dan Hanf</a>, and I released a game for the <a href="https://itch.io/jam/4mb-jam-2025">4MB game jam</a>. It was the first game jam for both of us, and the first game either of us had released.</p>
<h2>The 4mb game jam</h2>
<p>The <a href="https://itch.io/jam/4mb-jam-2025">4MB game jam</a> is a game jam that takes place every other year. The rules of the jam state that every submission must be a standalone desktop application and it must be below 4MB. This year, the theme was <em>&#39;Worms, Holes and Wormholes&#39;</em>, so we naturally decided to make a game about worms, and holes, and... you get the idea.</p>
<p>In the age of 100GB+ games, multi-dozen-MB web pages****, and <a href="https://nee.lv/2021/02/28/How-I-cut-GTA-Online-loading-times-by-70/">7+ minute loading times</a>, the idea of trying to make something fun with a much smaller footprint was compelling. The submissions included a full 3D FPS in just 21KiB, as well as a puzzle game written in hand coded NASM assembly (2.4KiB).</p>
<h2>Our game</h2>
<p><a href="https://morganherlocker.itch.io/worm-helsing">Itch.io / Worm Helsing</a></p>
<p>Worm Helsing is an isometric hack-n-slash where you explore a procedurally generated warren of vampiric worms as an intrepid explorer. We wrote the game in C using the <a href="https://www.raylib.com/">raylib</a> library. All audio was made by Dan in <a href="https://www.reaper.fm/">Reaper</a>, using a combination of synths and mic&#39;d sounds. I made the sprites and animations in <a href="https://www.aseprite.org/">Aseprite</a>, a simple pixel editing tool, with procedural effects sprinkled in to cut size.</p>
<p>In the end, we submitted a game that was 1,043 KiB, uncompressed, with no additional dependencies.</p>
<img src='/img/wormhelsing.gif' style='width:360px'>

<h2>Relative size breakdown</h2>
<p>Below is a <a href="https://windirstat.net/download.html">windirstat</a> view of the game&#39;s files. The executable was built for Linux Mint, and compressed with UPX. UPX has a long history in the world of demoscene, and works by compressing your program with a thin deflate wrapper that is unpacked at runtime. It has some downsides, in particular we found that Windows antivirus tries hard to prevent the user from executing a program like this, but in the case of a gamejam or demoscene submission, this is forgivable.</p>
<p><img src="/img/wormhelsing-linuxmint-windirstat.png" alt=""></p>
<p>As you can see, our exe was the largest component, followed by music. Some of the smaller submissions were able to exploit system resources, like SDL2, which comes preinstalled on Linux Mint, or Win32 for Windows. While I was very pleased with the size of our raylib binary, if we wanted to hit the sub-100KiB tiers, we would reconsider it for a library that could use one of these preinstalled dependencies. Some of the submissions got quite creative with their use of system dependencies, such as using a lib that uses the system web view, or a submission that relied on the system image viewer opened in a 2nd window.</p>
<h2>Takeaways</h2>
<ul>
<li>Creating a game from scratch is really fun! If you&#39;ve ever thought about it, I would recommend having a shot at a simple game you are confident you can ship.</li>
<li>UPX is helpful for cutting size by 25-60%, but it comes with security tradeoffs that make for a questionable tradeoff for serious apps.</li>
<li>Foundational dependencies you didn&#39;t write can far outweigh everything else you do write, for a timeboxed endeavor like this. We went in knowing the raylib dependency alone would be larger than some of the other submissions, and for us, it was worth it given our relative lack of experience shipping a full game.</li>
<li>Raylib was a joy to use, and I never got stuck on any API rough edge at any point. I very rarely even needed google, since reading the headers was enough - I wrote a good chunk of the game while offline in nature.</li>
</ul>
<hr>
<p><em>5-31-25</em></p>
]]></description></item><item><title>Raw bin</title><dc:creator>Morgan Herlocker</dc:creator><pubDate>Fri, 28 Mar 2025 07:00:00 GMT</pubDate><link>http://morganherlocker.com/post/Raw-bin</link><guid isPermaLink="true">http://morganherlocker.com/post/Raw-bin</guid><description><![CDATA[<h2>The Problem with protobuf</h2>
<p>I&#39;ll let Google describe the protobuf format:</p>
<blockquote>
<p>Protocol buffers are Google’s language-neutral, platform-neutral, extensible mechanism for serializing structured data – think XML, but smaller, faster, and simpler. You define how you want your data to be structured once, then you can use special generated source code to easily write and read your structured data to and from a variety of data streams and using a variety of languages.</p>
</blockquote>
<p>This is a compelling elevator pitch. Because protobuf stores schema data out of band from the data itself (unlike self-describing formats like JSON), data stored or sent over the wire does not include bloat from headers/field names/etc.</p>
<p>The downsides come in a few well-documented forms (not streamable, etc.), but I think the biggest issues are overlooked and unaddressed by alternatives:</p>
<ol>
<li>The generated code is large, and includes hundreds of thousands of lines from stdlib + many google libraries. This hurts compile times in a monorepo.</li>
<li>The format encourages array-of-structs style protocols to emerge.</li>
<li>It is non-trivial to write an alternative parser or serializer, due to unnecessary and complex features such as runtime reflection, nested data, etc.</li>
</ol>
<h3>Code size</h3>
<p>Let&#39;s take a common example of a data schema:</p>
<pre><code class="language-proto">syntax = &quot;proto3&quot;;
message LonLat {
  float lon = 1;
  float lat = 2;
}
</code></pre>
<p>This proto generates <strong>584 lines of C++, links to 173k lines of dependencies, and generates a 21Kb object file</strong> when compiled with <code>clang++@14.0.0-1ubuntu1.1</code>.</p>
<p>While the serialized data itself is only several bytes, the bindings to the generated parser outweigh the size of the payload, and are 2,625 times larger than the data we&#39;re trying to transmit for a single coordinate. At scale, the size of our data should far outweigh the size of the parser, but this adds up quickly when it comes to compile times and image sizes.</p>
<p>Do we need nearly 200k lines of code to parse two floats?</p>
<h2>Raw binary data</h2>
<p>Perhaps we don&#39;t need a parser at all. What if instead, we defined a lonlat protocol in a readme file that said something like this?</p>
<blockquote>
<p><em>LonLat file spec:</em></p>
<ul>
<li><em>Each file represents a list of strided lon, lat points.</em></li>
<li><em>All even values are lon, all odd values are lat.</em></li>
<li><em>The data type is 4 byte float.</em></li>
</ul>
</blockquote>
<p>A parser can now be written trivially, if we even want to call it a parser. It&#39;s just data.</p>
<pre><code class="language-js">const coords = new Float32Array(fs.readFileSync(&#39;./lonlat.bin&#39;));
for (let idx = 0; idx &lt; coords.length; idx++) {
    const lon = coords[0];
    const lat = coords[1];
    console.log(`coordinate: ${lon}, ${lat}`);
}
</code></pre>
<h3>Benefits</h3>
<p>We can confidently say that this &quot;parser&quot; can handle 1M+ coordinates at real-time visualization speeds in a browser, and 100M+ at ~1hz. There is no more efficient parser that could be written for simple coordinates in a browser without domain-specific compression tricks. Alternatives, including many uses of protobuf in clients, instead allocate memory into objects per item, which is considerably slower.</p>
<p>What&#39;s more, since the memory model is so simple with struct-of-arrays, we can easily estimate how much memory our programs will use with napkin math about the domain limits.</p>
<h3>Cross language &quot;support&quot;</h3>
<p>Outside a browser, our data format is also trivial to parse in any other language, because nearly every language has primitive support for parsing binary arrays of data quickly. For example, here&#39;s python:</p>
<pre><code class="language-python">coords = numpy.fromfile(&#39;./lonlat.bin&#39;)
</code></pre>
<hr>
<p><em>3-28-25</em></p>
]]></description></item><item><title>Control the runtime</title><dc:creator>Morgan Herlocker</dc:creator><pubDate>Fri, 07 Nov 2025 08:00:00 GMT</pubDate><link>http://morganherlocker.com/post/Control-the-runtime</link><guid isPermaLink="true">http://morganherlocker.com/post/Control-the-runtime</guid><description><![CDATA[<p>A dev mantra I&#39;ve come to believe is this:</p>
<blockquote>
<p>Control your runtime. Know why your code is running, when it&#39;s running, and how long it&#39;s running. Never let a dependency control your outer loop.</p>
</blockquote>
<p>In C, this usually looks something like this:</p>
<pre><code class="language-c">while(1) {
    // The things my server does all live here.
}
</code></pre>
<p>...or using raylib:</p>
<pre><code class="language-c">// raylib
while (!WindowShouldClose())
{
    // Update frame.
}
</code></pre>
<p>In JS, this might look something like this:</p>
<pre><code class="language-js">requestAnimationFrame(function (){
    // The things my page does all live here.
});
</code></pre>
<p>I&#39;m not too dogmatic about the particulars (Yes, I&#39;m technically at the mercy of the event loop in both the raylib and JS examples), but I <em>always</em> try to keep this &quot;while loop&quot; mental model in mind. <strong>Once a dependency takes over the outer loop, it becomes part of the platform you are building on</strong>, and comes with a much higher burden of reliability and performance. Dependencies that don&#39;t live up to this should be scrutinized and rejected.</p>
<p>Serious performance issues are rarely an intentional choice someone makes. They happen when a group of contributors lose track of where and why their code is running. Each block of code looks reasonable in isolation, but the sum of parts leads to frame drops, unexplained pauses, and wait times that can&#39;t be explained by reasoning about the compute and memory available.</p>
<p>Framework dependencies that promise to manage your runtime are tempting - automatic reactivity, magic horizontal scaling, managed DAG execution, etc. all seem to abstract away performance and coordination concerns. How do we scale complexity without it? <a href="https://cbarrete.com/carmack.html">John Carmack makes a related point about inlining code and the power of visibility and simple functional logic flow</a>:</p>
<blockquote>
<p>The real enemy addressed by inlining is unexpected dependency and mutation of state, which functional programming solves more directly and completely. However, if you are going to make a lot of state changes, having them all happen inline does have advantages; you should be made constantly aware of the full horror of what you are doing. When it gets to be too much to take, figure out how to factor blocks out into pure functions (and don&#39;t let them slide back into impurity!). </p>
</blockquote>
<hr>
<p><em>11-16-25</em></p>
]]></description></item><item><title>globe.c</title><dc:creator>Morgan Herlocker</dc:creator><pubDate>Thu, 27 Feb 2025 08:00:00 GMT</pubDate><link>http://morganherlocker.com/post/globe.c</link><guid isPermaLink="true">http://morganherlocker.com/post/globe.c</guid><description><![CDATA[<p>I recently decided to <a href="https://github.com/morganherlocker/globe.c">write a parser</a> for NOAA&#39;s Global Land One-kilometer Base Elevation (GLOBE) dataset. Compiled in 1999, GLOBE is a coarse resolution dataset, suitable for climate/weather modelling, non-safety-critical navigation aids, and other tasks where a finer-grained model is not necessary. This dataset is a convenient format for projects that need to approximate the geological features of the Earth on a global scale while maintaining a lightweight memory footprint. 1km resolution is higher than the resolution of most weather models, such as the Global Forecast System, which has a 28km horizontal resolution, and it is a good starting point for applications like games and global basemaps.</p>
<h2>Size</h2>
<p>Weighing in at 1.8GB raw, GLOBE has a simple file format. The dataset is divided into 18 spatial partitions, each representing a 30 arc-second grid of lon/lat centroids. The files each contain a block of 16 bit signed integers (int16_t), making it straightforward to load in any language that can read contiguous arrays from disk. For example, in python, you could do something like:</p>
<pre><code class="language-py">np.fromfile(&#39;./a10g&#39;, dtype=np.int16)
</code></pre>
<p>...or in node.js:</p>
<pre><code class="language-js">new Int16Array(fs.readFileSync(&#39;./a10g&#39;))
</code></pre>
<p>This is a common way to pass assets around in game development, but not so common anywhere else these days. Most modern geospatial file types have complex header schemas, hierarchical structures, and nested format types. GLOBE follows an older model, where the &quot;header&quot; is a detailed PDF specification describing the data, and the data is just... the data. Refreshingly simple!</p>
<h2>Partition structure</h2>
<p>The file name is used to convey metadata about each file. The first character of the file name represents the spatial partition, followed by a version number. So <em>a11g</em> is the Alaska partition, version 11. NOAA provides the following image to explain this structure:</p>
<p><img src="/img/globe-grid.png" alt=""></p>
<h2>globe.c</h2>
<p>globe.c is a parser and toolset for NOAA&#39;s Global Land One-kilometer Base Elevation (GLOBE) dataset. It has zero dependencies and is written in C99. It is capable of flattening the raw grids into a single file, making it easier to load and manipulate in memory. <strong>While the spatial sharding was a necessary convenience in 1999, <em>today we can easily fit the entire dataset into a single file on disk, and even a single array in memory.</em></strong> This makes it possible to load the entire globe in a single line of code in most programming languages without the need for an API of any kind.</p>
<pre><code class="language-sh">globe merge -o ./globe.bin;
node -e &quot;console.log(new Int16Array(fs.readFileSync(&#39;./globe.bin&#39;)))&quot;
    Int16Array(1866240000) [
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254, 12, 254, 12, 254, 12, 254, 12, 254,
        12, 254, 12, 254,
    ... 1866239900 more items
]
</code></pre>
<p><a href="https://morganherlocker.github.io/globe.c/">A demo site is available for the project</a>, including a prerendered world map. The provided map is powered by Leaflet, backed by tiles rendered using globe.c, following a standard map tile structure:</p>
<pre><code>globe render -i ./globe.bin -o ./demo/1/0/0.png --minlon=-180 --minlat=0 --maxlon=0 --maxlat=90;
globe render -i ./globe.bin -o ./demo/1/0/1.png --minlon=-180 --minlat=-90 --maxlon=0 --maxlat=0;
[...]
</code></pre>
<p><img src="/img/leaflet-globe.png" alt=""></p>
<h2>Querying</h2>
<p>globe.c can export data as CSV, making it easy to query and analyze. In this example, the csv data is converted to parquet for further analysis using <a href="https://duckdb.org/">duckdb</a>.</p>
<pre><code class="language-sh">globe table -i ./globe.bin -o globe.csv;
duckdb -c &quot;copy (select * from globe.csv) to &#39;globe.parquet&#39; \
(FORMAT PARQUET, COMPRESSION ZSTD, ROW_GROUP_SIZE 10_000_000)&quot;
</code></pre>
<p>Summary statistics:</p>
<pre><code class="language-sql">select 
    min(elev), 
    quantile_disc(elev, 0.25) as p25, 
    median(elev), 
    avg(elev), 
    quantile_disc(elev, 0.75) 
    as p75, max(elev) 
from &#39;./globe.parquet&#39;;

# Summary
+-----------+-----+--------------+-------------------+------+-----------+
| min(elev) | p25 | median(elev) |     avg(elev)     | p75  | max(elev) |
+-----------+-----+--------------+-------------------+------+-----------+
| -407      | 219 | 609.0        | 1138.489993141472 | 1950 | 8752      |
+-----------+-----+--------------+-------------------+------+-----------+
</code></pre>
<p>Locating the high and low point:</p>
<pre><code class="language-sql">select * from globe.parquet order by elev desc limit 1;

# Himalayas
+-----------+----------+------+
|    lon    |   lat    | elev |
+-----------+----------+------+
| 86.874268 | 28.00589 | 8752 |
+-----------+----------+------+

select * from globe.parquet order by elev asc limit 1;

# Dead Sea
+-----------+-----------+------+
|    lon    |    lat    | elev |
+-----------+-----------+------+
| 35.320187 | 30.997511 | -407 |
+-----------+-----------+------+
</code></pre>
<p>The source code is provided under the <a href="https://github.com/morganherlocker/globe.c/blob/main/LICENSE">MIT license</a>.</p>
<hr>
<p><em>2-27-25</em></p>
]]></description></item></channel></rss>
